{
  "name": "blue-green-deployment-action",
  "version": "1.0.0",
  "description": "GitHub Action for managing blue-green deployments using AWS DynamoDB",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src/ --ext .js",
    "lint:fix": "eslint src/ --ext .js --fix",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-org/blue-green-deployment-action.git"
  },
  "keywords": [
    "github-actions",
    "blue-green-deployment",
    "aws",
    "dynamodb",
    "ci-cd",
    "deployment"
  ],
  "author": "Your Organization",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/your-org/blue-green-deployment-action/issues"
  },
  "homepage": "https://github.com/your-org/blue-green-deployment-action#readme",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@aws-sdk/client-dynamodb": "^3.863.0",
    "@aws-sdk/lib-dynamodb": "^3.863.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}